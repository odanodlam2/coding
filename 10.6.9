def remove_all_from_string(word, letter):
    while letter in word:
    	for let in word:
          if let == letter:
              index = word.find(let)
              word = word[:index] +  word[index+1:]
    return word
    
