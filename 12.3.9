# (NOTE, REMOVE) this code just doesnt work but while debugging this worked for some reason lol
def getindices(): 
    sentence = input('They occured at indices: ')
    for i, word in enumerate(sentence):
        print(i)
print(f"They occured at indices: {getindices()}")
