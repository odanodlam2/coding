def replace_at_index(string, index):
    return # update the function to return `word` with all instances of `letter` removed!
def remove_all_from_string(word, letter):
	return word[:3] + word[4:]
        
remove_all_from_string("python", "n")
